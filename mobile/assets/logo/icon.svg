<svg width="1024" height="1024" viewBox="0 0 1024 1024" xmlns="http://www.w3.org/2000/svg">
  <!--
    App icon derived from the login welcome photo so the icon, splash, and
    login screen share the same imagery. {{PHOTO}} is replaced with a base64
    data URI by render.mjs at build time.
  -->
  <defs>
    <clipPath id="frame">
      <rect width="1024" height="1024"/>
    </clipPath>
    <linearGradient id="tint" x1="0%" y1="0%" x2="0%" y2="100%">
      <stop offset="0%" stop-color="#0a0f1c" stop-opacity="0.10"/>
      <stop offset="100%" stop-color="#0a0f1c" stop-opacity="0.40"/>
    </linearGradient>
    <radialGradient id="vignette" cx="50%" cy="50%" r="75%">
      <stop offset="55%" stop-color="#000000" stop-opacity="0"/>
      <stop offset="100%" stop-color="#000000" stop-opacity="0.50"/>
    </radialGradient>
  </defs>
  <g clip-path="url(#frame)">
    <!-- Photo scaled to height=1024 (preserves 1600:1067 aspect), shifted left
         so the paddle face + ball sit visually centered in the square crop. -->
    <image href="{{PHOTO}}" x="-440" y="0" width="1535" height="1024" preserveAspectRatio="none"/>
    <rect width="1024" height="1024" fill="url(#tint)"/>
    <rect width="1024" height="1024" fill="url(#vignette)"/>
  </g>
</svg>
